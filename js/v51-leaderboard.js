(()=>{"use strict";
const PROFILE_KEY="f2math_v50_profiles",ACTIVE_KEY="f2math_v50_active_student";
const $=s=>document.querySelector(s);
function profiles(){try{return JSON.parse(localStorage.getItem(PROFILE_KEY)||"{}")||{}}catch{return {}}}
function score(p){const bosses=Object.values(p.bossWins||{}).filter(Boolean).length,correct=p.stats?.correct||0,wrong=p.stats?.wrong||0,accuracy=correct+wrong?Math.round(correct/(correct+wrong)*100):0,stars=Object.values(p.masteryStars||{}).reduce((a,b)=>a+(Number(b)||0),0);return (p.xp||0)+bosses*100+stars*40+accuracy*2+(p.bestCombo||0)*10}
function inject(){const row=$("#lobbyView .topbar .row");if(row&&!$("#leaderboardBtn")){const b=document.createElement("button");b.id="leaderboardBtn";b.className="btn secondary";b.textContent="Leaderboard";row.insertBefore(b,$("#teacherLobbyBtn")||$("#switchBtn"));b.addEventListener("click",open)}if(!$("#leaderboardView")){const main=$("main.shell"),section=document.createElement("section");section.id="leaderboardView";section.className="panel hidden";section.innerHTML='<div class="topbar"><div><strong>Local Leaderboard</strong><div id="leaderboardSummary" class="muted"></div></div><button id="leaderboardBackBtn" class="btn secondary">Back to Chapters</button></div><div id="leaderboardPodium" class="leaderboard-podium"></div><div id="leaderboardList" class="leaderboard-list"></div>';main.insertBefore(section,$("#gameView"));$("#leaderboardBackBtn").addEventListener("click",back)}}
function hide(){["loginView","lobbyView","heroView","petView","missionView","achievementView","teacherView","shopView","chestView","masteryView","gameView","wrongBookView"].forEach(id=>$("#"+id)?.classList.add("hidden"))}
function open(){hide();$("#leaderboardView").classList.remove("hidden");render()}
function back(){$("#leaderboardView")?.classList.add("hidden");$("#lobbyView")?.classList.remove("hidden")}
function render(){const all=Object.values(profiles()).map(p=>{const correct=p.stats?.correct||0,wrong=p.stats?.wrong||0,attempts=correct+wrong,bosses=Object.values(p.bossWins||{}).filter(Boolean).length,stars=Object.values(p.masteryStars||{}).reduce((a,b)=>a+(Number(b)||0),0);return {...p,rankScore:score(p),accuracy:attempts?Math.round(correct/attempts*100):0,bosses,stars}}).sort((a,b)=>b.rankScore-a.rankScore||b.xp-a.xp||a.name.localeCompare(b.name));const active=localStorage.getItem(ACTIVE_KEY),myRank=all.findIndex(p=>p.name===active)+1;$("#leaderboardSummary").textContent=`${all.length} student${all.length===1?"":"s"}${myRank?` · Your rank #${myRank}`:""}`;const medals=["🥇","🥈","🥉"];$("#leaderboardPodium").innerHTML=all.slice(0,3).map((p,i)=>`<article class="podium-card place-${i+1}"><div class="podium-medal">${medals[i]}</div><strong>${p.name}</strong><span>${p.rankScore} pts</span><small>${p.bosses} Boss · ${p.stars} Stars · ${p.accuracy}%</small></article>`).join("")||'<div class="empty-state">No student records yet.</div>';$("#leaderboardList").innerHTML=all.map((p,i)=>`<article class="leaderboard-row ${p.name===active?'current':''}"><b>#${i+1}</b><div><strong>${p.name}</strong><small>XP ${p.xp||0} · Combo ${p.bestCombo||0}</small></div><span>${p.rankScore} pts</span><small>${p.accuracy}% accuracy</small></article>`).join("")}
window.addEventListener("DOMContentLoaded",()=>{document.title="F2 Math Hero V51.5";const h=$(".hero h1");if(h)h.textContent="F2 Math Hero V51.5";const p=$(".hero p");if(p)p.textContent="Learning RPG with heroes, pets, battles, mastery, rewards, leaderboards and teacher tracking.";inject()});
})();